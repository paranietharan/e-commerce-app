spring:
  data:
    mongodb:
      username: paranie
      password: paranie
      additional-hosts: localhost
      port: 27017
      database: customer
      authentication-database: admin

server:
  port: 8090